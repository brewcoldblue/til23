SelectionSort(arr)
	arr: 주어진 배열
	n: 배열의 길이
	n-1: pass의 개수
	
	for i from 0 to n-2
		idx = i; //매 패스마다 첫 번째 원소를 최소값으로 가정
		
		for j from i+1 to n-1 //정렬된 원소를 제외한 배열을 순회
			if (A[j] < A[idx]) idx = j; //더 작은 값이 나오면 인덱스 바꿔주고
			
		swap(A[idx], A[i]) 패스의 첫 번째 원소와, 최소값으로 판명된 값을 바꿔준다
			